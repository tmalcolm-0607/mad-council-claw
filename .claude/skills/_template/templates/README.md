# Template-skill templates

Per-skill structured artifact shapes. Replace this directory's contents when forking.

For shared shapes (idea.md, spec.md, plan.md, tasks.md), reference `.mad/templates/` instead of duplicating here.
