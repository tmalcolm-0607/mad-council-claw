# 01 — Requirements

Foundational context every contributor (human, Claude Code instance, Copilot CLI session, subagent) MUST read before doing any work in this repo.

## Contents

- `foundational-plan.md` — the full plan file authored at session start. Verbatim.
- `session-requests.md` — Messages 1-33 from the originating chat session, verbatim. The user's words; no synthesis.
- `goals.md` — Goals G1-G25, each citing the message it derives from.
- `glossary.md` — source-tag legend, confidence labels, key abbreviations.

## Non-negotiable

- Per Message 5/30/31/33: don't retrofit, don't synthesize, don't pivot. The plan is the requirements; the requirements are additive across the session, not sequential overrides.
- Per Goal G24: pre-compaction context is preserved here so future instances start from the same baseline.
