{
  "name": "mad-council-claw",
  "version": "0.0.0",
  "private": true,
  "description": "MAD Council Claw — hybrid Electron+headless AI agent platform combining clawpilot UX with canonical-e governance discipline",
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:node": "vitest run tests/node",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "build": "echo 'no build yet — F-001 is library-only at this point' && exit 0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/ui": "^2.0.0",
    "happy-dom": "^15.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "@mad-council-claw/engine-core": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
